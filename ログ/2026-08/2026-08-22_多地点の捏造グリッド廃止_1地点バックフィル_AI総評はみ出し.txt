===========================================================
2026-08-22 セッション
レポート指摘3件: ①AI総評の文字はみ出し ②多地点のずれ ③過去月をシート順位1地点で保存
===========================================================

## 指摘（スクショ2枚）
1. 月次推移ページのAI総評が下端で切れている（「…維持できている。一方ウェブサイトクリック
   とフードメニュークリックは前月比でそれぞれ-20%、-23%と落ち込んでおり、」で見切れ）
2. 多地点順位ページ（クインシー・2026/2）の地図がずれ、49地点すべて「-」
3. 5拠点=8/1計測(7月分) / 7/1計測(6月分)は7×7 / 5月分より前は未計測なので
   シートから順位を取得して保存・反映（レポートの反映は1地点のみ）

## 調査でわかった実データ（本番DB直結）
- クインシーの grid_ranking_logs: 2026/5=7×7(6/2計測)、2026/6=7×7(7/1計測)、
  2026/7=5地点(8/1計測)。overridesは0件 → 帰属月(report_month)は指摘どおり正しい
- 全体: grid_ranking_logs のある稼働店舗 335件 / 実測月は 2026/4:4, 5:130, 6:3972, 7:958
- ★1月・2月・4月のグリッドは実測ではなく **コードが捏造していた**
  supplementGridFromRanking() の generateSimpleGrid が、シートの順位1個から
  keyword|month|row|col のハッシュで48地点分の順位を作り、7×7に見せていた
  （2026-06-20にMath.random()→決定論ハッシュ化したが、捏造である点は残っていた）
  座標も持たない（lat/lng=0）ため、画面側が店舗座標から1km間隔の仮グリッドを描いていた

### 決定事項
- 捏造7×7は廃止。実在するのはシートの1順位だけなので gridSize=1 の1地点にする
- シートで「圏外」と明記された月は rank=-1 で計測済み扱い（空欄＝未計測は保存しない）
- 保存対象は「その店舗の最古の実測月より前の月」だけ。
  後から実測が入り得る月にoverrideを置くと、overrideが実測を隠すため（読み取り側は override > logs）

## 変更内容（コミット 0aa7c0d / origin main へpush済み）
■ src/lib/report-api.ts
  - supplementGridFromRanking: generateSimpleGrid（49地点捏造）を削除し
    singlePointGrid（1地点）に置換。gridSize=1 / intervalM=null / avgRank=順位
  - datasets.outOfRange を見て「圏外」を rank=-1 で補完（未計測と区別）
■ src/lib/report-utils.ts
  - gridLayoutLabel: gridSize=1 or 1点 → 「1地点」（従来は "1×1" と出ていた）
■ src/app/report/[shopId]/client.tsx
  - ★地図のずれ修正: 同じdivに new google.maps.Map() を毎回重ねていたため、
    前インスタンスのタイルがDOMに残り、新インスタンスのマーカーだけが別の中心・
    ズームで描かれていた。マップ本体を使い回す（div違いのときだけ作り直し＋innerHTML=""）
  - 1地点の月は fitBounds ではなく setZoom(15)（1点だとfitBoundsが最大ズームに張り付く）
  - KWタブ切替時に地図を再描画（非表示スライドは幅0で初期化されるため縮尺が合わない）
  - AI総評のはみ出し対策: .page-comment に対し、収まるまで 13px→9px に0.5px刻みで自動縮小。
    総評内の文字は fontSize:"inherit" に変更（p/ul/labelが13px固定だと縮まないため）
  - 1地点の月がある場合、平均順位表の下に「1地点の月は多地点計測を開始する前」の注記
■ src/app/api/report/analyze/route.ts
  - AI総評の入力から results.length<=1 のスナップショットを除外
    （1点から「圏外0地点＝商圏の取りこぼし無し」と作文させない）
■ src/lib/spreadsheet.ts
  - overrides経路のKWを normalizeKw で正規化。overridesは正規化済み・logsは生のままなので、
    同じKWが「表参道 カフェ」「表参道　カフェ」の2行に割れて出る事故を防止

## 本番DBへの投入（grid_ranking_overrides）
- 投入: 14,347行 / 327店舗（dry-run 14,387行から重複40件を後勝ちで排除）
  - 内訳: 順位あり 13,470 / 圏外(-1) 917
  - 月: 2025/3〜2026/6（2026/4:1862, 2026/3:1594, 2026/2:1777, 2026/1:1719 など）
  - 1行 = grid_size:1, results:[{row:0,col:0,lat,lng,rank}]（座標はshops.gbp_latitude/longitude）
- 既存の手動override 87行（grid_size=7・2店舗）は shop×月×KW で除外済み・無傷
- 実測月との衝突 0件（投入前に検証）
- ★取り消しは `delete from grid_ranking_overrides where grid_size = 1`
  （既存overrideは全て grid_size=7 なので、今回投入分だけを正確に消せる）
- スクリプト: backfill-grid-sheet.ts（シート取得→plan生成。tsx実行・dry-runが既定）
  apply-plan.js（plan投入・重複除去・二重投入ガード）／ grid-backfill-plan.json（投入内容）

## 数値・設定値
- AI総評の自動縮小: 13px（基準）→ 9px（下限）、0.5px刻み、描画600ms後に測定
- 1地点マップのズーム: 15
- シート取得: 335店舗を並列5で約6分（gviz CSV・認証不要）

## 注意点・ハマったこと
- shops.id は text、grid_ranking_logs.shop_id は uuid、grid_ranking_overrides.shop_id は text。
  JOINは `s.id = g.shop_id::text`（逆向きにキャストすると "operator does not exist" で落ちる）
- シート順位に 100位超が29行（14店舗）ある。最大は エミナルクリニック福岡天神院 の
  「福岡市 脱毛」2026/5 = 1129位。捏造グリッドは >100 を0（未計測）に丸めていたので
  今までグリッドには出ていなかったが、P7のキーワード順位表には元から出ている。
  シート側の入力ミスの可能性が高い → 要確認（今回はシートのまま保存した）
- 1地点の月と多地点の月が同じ推移表に並ぶため、計測方式が変わった段差を
  順位変動と誤読される。注記を出したが、根本的には「1地点の月は別系列」にすべきか要検討
- 地図のずれは Chrome拡張が未接続で本番画面での再現確認ができていない。
  new Map() の重ね掛けは実コード上の明確な誤りなので修正したが、
  デプロイ後に画面で要確認（特に月/KWを何度か切り替えた後）

## 未解決・次回やること
- デプロイ後の目視確認: ①クインシー2026/2が1地点表示になっているか
  ②地図のピンが店舗位置に合っているか ③AI総評が切れずに収まっているか（PDF出力も）
- 100位超29行の妥当性をシート担当に確認
- 8/16分の残タスクは未着手のまま（sync-activityのオフセット、Batch32件、複数月まとめ検証）

===========================================================
【追記】同日 グラフ可変化 + コードレビュー対応
===========================================================

## グラフを可変高にしてAI総評の場所を確保（86c2c68）
- ユーザー指摘「グラフは小さくてよい。AI総評の文字が小さい方が具合悪い」
- Chart.js の maintainAspectRatio を false にし、コンテナを flex:1 / minHeight:200 に。
  従来は横幅からグラフ高が決まるため、総評が長い月に794pxのスライドから溢れていた
- 対象5ページ: P3マップ表示数 / P4検索数 / P5ユーザー反応数 / P8口コミ件数 / P9月間増加数
- 検証: 同構造・同データのテストページをヘッドレスChromeで描画。
  総評4倍(11行)でも13pxのまま全文表示・グラフが縮むだけ（chrome --headless=new --screenshot）
- 一時スクリプトを「一時スクリプト/」へ移動し tsconfig の exclude に追加。
  プロジェクト直下に.tsを置くと next build が pg の型解決で落ちる

## PDF直前の再測定（5e3bc65）
- 読み込み直後にPDFを押すと自動縮小(描画600ms後)の前に印刷され、文末が切れる可能性
- fitPageComments を切り出し、waitForPhotos の後に呼ぶ

## コードレビュー指摘の修正（b529d25）
1. ★圏外センチネルの不一致（重要）
   - 実測 grid_ranking_logs は圏外を rank=0 で保持（実測: 0が55,142件 / 負は12件のみ）
   - report-data.ts のコメントは「0=未計測 / -1=圏外」だが、実データは真逆
   - 当初 -1 で入れたため analyze/route.ts:716 `center?.rank || 0` を truthy で通過し、
     シート順位フォールバックが働かず -1 が kwData.rank に混入していた
   - コードと本番900行を 0 に統一（jsonb_set、トランザクション）
2. 管理画面の多地点グリッド編集が1地点の月で壊れる
   - 7×7固定の盤面に1点だけ左上に入り、中心(3,3)が「-」。セル押下は404で保存失敗
   - gridCells.length > 1 の時だけ盤面を出し、1地点の月は順位と自動生成の案内に変更
3. KWタブ切替時の地図再描画が飛ぶ
   - 描画側は Math.min(gridKwIdx, 件数-1)、再描画側は生index → KWを隠した直後に undefined
4. spreadsheet.ts フォールバック経路(fetchGridRankingData)のKW未正規化
5. 一時スクリプトを .gitignore へ（DB認証情報を含む）
6. vitest testTimeout 5s→20s
   - 全体実行時のみ supabase.test.ts が5秒タイムアウト。単体では通るため誤解を招く
   - 変更前のツリーでも同じ1件が落ちることを stash して確認済み（今回の変更が原因ではない）

## 検証結果
- 本番バンドルに新コードが載っていることを確認（page-373cfce4db60acbc.js に
  「多地点計測を開始する前」「maintainAspectRatio:!1」×3「minHeight:200」）
- 投入14,347行の健全性: 実測を隠す0件 / 形式異常0 / 座標なし0 / shop_id・name不整合0 /
  解約店舗0 / 重複0（DBのunique index (shop_name,keyword,month) とも整合）
- KW増加は327店中100店・最大+5（大半+1〜2）
- tsc / next build / vitest 451件 すべてパス

## 残タスク
- 画面での目視確認（1地点表示・地図・総評の収まり・PDF）
- シート順位の100位超29行（最大1129位：エミナルクリニック福岡天神院）の妥当性確認
- MEOマスタ同期（/admin）はユーザー判断で保留中

===========================================================
【追記3】セッション終了サマリー（規定フォーマット）
===========================================================

## 【2026-08-22】

### 決定事項
- 多地点順位の「捏造7×7」を廃止する。実測が無い月に出していた49地点は、シートの
  順位1個からハッシュ生成した架空値だった。実在するのはシート順位1つだけなので
  gridSize=1 の「1地点」として扱う
- 過去月のシート順位は grid_ranking_overrides にDB保存する（表示時に毎回作らない）。
  保存範囲は「その店舗の最古の実測月より前の月」だけ。後から実測が入り得る月に
  overrideを置くと、読み取り優先度が override > logs のため実測を隠す
- 圏外のセンチネルは rank=0 に統一する。実測ログが圏外を0で持っている（0が55,142件、
  負は例外12件）ため。report-data.ts のコメント「0=未計測/-1=圏外」は実データと逆
- 空欄（未計測）は保存しない。「圏外」と明記されたセルだけ計測済みとして保存する
- グラフとAI総評が競合したらグラフを縮める。文字を小さくするのは最後の保険
  （ユーザー指示「グラフは小さくても分かる。AI総評の文字が小さい方が具合悪い」）
- 多地点の地図は「表示中のKWだけ」「毎回作り直す」。使い回しとオフスクリーン生成を
  やめる
- MEOマスタ同期（シートB列→解約 / C列に無い店舗→計測対象外）は既存機能
  （/admin の契約ステータス同期）で実施できる。今回は**ユーザー判断で保留**。DBへの
  書き込みは行っていない
- 16件の「マスタにあるがシステムに無い店舗」は一覧提示のみ。登録・名寄せは保留

### 変更内容（具体的に）
■ src/lib/report-api.ts
  - supplementGridFromRanking: generateSimpleGrid（49地点をハッシュ生成）を削除し
    singlePointGrid（1地点）に置換。gridSize=1 / intervalM=null / avgRank=順位
  - rankingHistory の型に outOfRange?: boolean[] を追加し、「圏外」セルを rank=0 で
    計測済み扱いに（空欄はスキップ）
■ src/lib/report-utils.ts
  - gridLayoutLabel: gridSize===1 または点数1 → 「1地点」を返す（従来は "1×1"）
■ src/lib/spreadsheet.ts
  - fetchAllExternalData の overrides経路: keywords / snapshot.keyword を normalizeKw
  - fetchGridRankingData（フォールバック経路）も log.keyword を normalizeKw
■ src/app/api/report/analyze/route.ts
  - AI総評の入力から results.length <= 1 のスナップショットを除外（1点から商圏を
    語らせない）
■ src/app/report/[shopId]/chart-options.ts
  - buildStackedOptions / lineOptions の maintainAspectRatio を true→false、
    aspectRatio:2.5 を削除
■ src/app/report/[shopId]/client.tsx
  - グラフのコンテナを flex:"1 1 0" / minHeight:200 / position:relative に（P3〜P5、P8、P9）
  - P9のインラインoptionsも maintainAspectRatio:false
  - renderPageComment: ラッパに className="page-comment" と fontSize:13、内側の
    p/ul/label を fontSize:"inherit" に
  - fitPageComments を新設（総評が溢れる場合だけ13px→9pxへ0.5px刻みで縮小）。
    useEffect（描画600ms後）と handlePdfDownload の両方から呼ぶ
  - renderGridMapForKw: マーカー消去を「データ無し判定」より前へ移動／マップは
    毎回 innerHTML="" + new Map() で作り直し／1地点は fitBounds ではなく setZoom(15)
  - 地図描画のuseEffect: 全KWループ → 表示中KWのみ。deps に visibleGridRanking と
    gridKwIdx を追加
  - 多地点サマリーページに「1地点の月は多地点計測を開始する前」の注記を追加
  - 管理画面のグリッド編集: 盤面を出すのは gridCells.length > 1 のときだけ。
    1地点の月は順位と「7×7にするには自動生成」の案内に変更
  - 「この月のデータなし」の枠に width:440 を指定
■ vitest.config.ts … testTimeout 20000 を追加
■ tsconfig.json … exclude に "一時スクリプト" を追加
■ .gitignore … 一時スクリプト/ と一時js・tsvを追加
■ 本番DB（grid_ranking_overrides）
  - 1地点データ 14,347行 / 327店舗を投入（後から圏外900行を -1 → 0 に更新）

### 数値・設定値
- 投入: 14,347行（dry-run 14,387行から重複40件を後勝ちで除外）／327店舗／
  月範囲 2025/3〜2026/6／順位あり13,470・圏外900
- グラフ: minHeight 200px、maintainAspectRatio false
- AI総評の自動縮小: 基準13px → 下限9px、0.5px刻み、描画600ms後に測定
- 1地点マップのズーム: 15（fitBoundsだと最大ズームに張り付くため）
- vitest testTimeout: 5000 → 20000ms
- シート取得: 335店舗を並列5で約6分（gviz CSV・認証不要）
- 多地点の実測カバレッジ: 6月分 332/335店、**7月分 183/335店**
  （7月分は8/1に182店。152店が未計測）
- MEOマスタ同期のdry-run: 契約ステータス変更2件／マスタ未掲載167件／復帰7件／
  変更比率21.5%（30%超でforce必要）

### 未解決・次回やること
- 【要判断】7月分の多地点計測が152店舗で未実施。8/1の一括計測が全店に回っていない
- 【保留中】MEOマスタ同期（/admin の契約ステータス同期）。適用すると
  解約1・停止中1・マスタ未掲載167件。適用前に SPICE CURRY CEYLON HOUSE と
  うら山本店 の名前違いを直さないと、稼働中の店舗が計測対象外になる
- 【保留中】マスタにあってシステムに無い16店舗の登録・名寄せ
- シート順位の100位超29行（最大1129位：エミナルクリニック福岡天神院「福岡市 脱毛」
  2026/5）の妥当性確認
- デプロイ後の目視確認（KWタブ・月タブの往復、特にデータの無い月を経由する操作）
- 8/16分の残タスクは未着手（sync-activityのオフセット、Batch32件、複数月まとめ検証）

### 注意点・ハマったこと
- ★オフスクリーン（left:-99999px）で生成したGoogle Mapsは fitBounds が効かず
  初期zoomのまま固定される。ヘッドレスChromeで実測（オフスクリーン生成 zoom=13 /
  表示中生成 zoom=12）。非表示スライドで地図を作ってはいけない
- ★同じdivに new google.maps.Map() を重ねると旧インスタンスのタイル層(.gm-style)が
  DOMに残る。作り直す前に innerHTML="" が必須
- ★マーカーの消去は「この月にデータが無い」判定より前に置く。後ろだと早期returnで
  消し損ね、前の月のピンが残る（「2026/3なのに7×7のピン」の正体）
- ★圏外センチネルはコメントと実データが逆。-1で入れると analyze/route.ts の
  `center?.rank || 0` をtruthyですり抜け、シート順位フォールバックが働かない
- 型が混在するJOIN: shops.id=text / grid_ranking_logs.shop_id=uuid /
  grid_ranking_overrides.shop_id=text。`s.id = g.shop_id::text` が正しい
  （逆向きは "operator does not exist: text = uuid"）
- grid_ranking_overrides には unique index (shop_name, keyword, month) がある。
  一括投入前に重複除去が必須（シートに同じKWが2行あるケースが40件あった）
- プロジェクト直下に .ts の一時スクリプトを置くと next build が pg の型解決で落ちる。
  tsconfig の exclude に入れること
- JSXの三項演算子の「:」直後に {/* コメント */} を置くと子要素が2つになり構文エラー
- vitest は全体実行時だけ supabase.test.ts が5秒タイムアウトで落ちる。単体では通る。
  変更前のツリーでも落ちることを git stash で確認してから timeout を伸ばした
- 検証に本番画面を開けない（Chrome拡張が未接続・Playwrightは他セッションが専有）。
  代替として「同じ構造のテストHTMLをヘッドレスChromeで描画→スクショ/計測値を読む」
  が有効だった。Google Maps APIキーは本番バンドルから取得できる（NEXT_PUBLIC_）
- 「解約」はDB側で既に35/37件反映済みだった。シートとDBの差分は思ったより小さい
