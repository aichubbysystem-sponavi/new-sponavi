## 【2026-07-31】レポートPDF（patty rôti 2026/6分）のレビューと指摘全件修正

対象PDF: C:\Users\pasok\Downloads\レポート _ SPOTLIGHT NAVIGATOR _ SPOTLIGHT NAVIGATOR.pdf（20ページ）
※PDFの読み取りは PyMuPDF (python -c "import fitz") でページを画像化して確認した。
　poppler(pdftoppm)は未インストールなので Read ツールのPDF直読みは不可。

### 発見した不具合（重大度順）

■【最重大】圏外/未計測への転落を「順位改善」として表示していた
- 症状: 「一社 イタリアン」4月1位→6月データなし で 変動「↑6」（緑・上昇）
        多地点でも 4月8.3→6月データなし で「↑6.2」（緑）
        「名東区 パスタ」は「→」（横ばい）
        一方P6のカードは同じKWを「圏外」＋赤↓ → 同一レポート内で結論が真逆
- 真因: client.tsx で null（データなし）を除外してから配列末尾2件を比較していた
        ranks=[3,7,null,1,null] → 除外後[3,7,1] → 2月(7位)と4月(1位)の差=6 を
        「当月の変動」として表示。当月がデータなしでも過去2ヶ月の差分が出てしまう
- 該当4箇所（すべて同じパターン）:
    P7 順位推移テーブル / P8 多地点サマリー右表 / KW別スライドの月別表 / PDF生成側の平均順位
- 対策: report-utils.ts に rankTrend() を新設。必ず「最新列」を基準にし、
        比較相手だけを過去へ遡って探す。4箇所すべてを置き換え。
        旧 avgRankDiff() は隣接2値しか見られず未計測を表現できないため @deprecated に。

■「圏外」と「未計測」を区別できていなかった（上記の派生）
- ranking-fetch.ts の parseRanksHistory / parseRanksHistorySheet3 が
  セル「圏外」も空欄も同じ null に潰していたため、区別不能だった
- 対策: RankHistoryData / RankingHistory の datasets に outOfRange?: boolean[] を追加
        （既存フィールドは変更しないので他の消費側に影響なし）。
        mergeHistories・対象月トリムでも引き継ぐ。
        client側の measured 判定 = グリッド計測あり OR シートが明示的に「圏外」
- 表示: 計測済みで順位なし→「圏外へ」（赤）／未計測→「未計測」（灰・矢印なし）

■ 平均順位が圏外を分母から除外していた
- avgRank は report-api.ts / spreadsheet.ts で results.filter(r=>r.rank>0) の平均。
  → 名東区カフェは49地点中45地点が圏外でも「平均26.5位」と表示され実態を誤認させる
- 対策: rankCoverage() を新設し、圏内率（16/49 等）を
        サマリー表・KW別スライド・PDF地図スライドすべてに併記。50%未満は赤で強調

■ 前年比が計測開始前の断片データで計算されていた
- 対策開始2025年8月／前年同月2025/6は マップ0件・検索4件しかない
  → 「+116325.0%（4→4,657）」「+108,951（0→108,951）」と表示
- 対策: isYoyComparable(yoyValue, curLabel, startDate) を新設。
        前年同月が対策開始月より前、または0以下なら前年比バッジを出さず
        「前年比 なし（前年は計測前）」と理由を書く。
        検索語句テーブルの「前年」「前年比」列も同条件＋共通語句なしで列ごと非表示に
        （PDFでは全行「-」の空列が2列出ていた）

■ データなしKWの地図が真っ白なスライドで出ていた（P11/P12）
- 真因: PDF生成ループが gr.keywords 全件ぶんスロットを作るが、
        対象月のスナップショットが無いと if ブロックごとスキップされ
        プレースホルダ（空の箱＋「平均順位: -位」）が残る
- 対策: 対象月に results があるKWだけを pdfKwEntries に絞ってスライド生成。
        キャプチャループも同じ配列を回し、slotIdx（スロット順）と kwIdx（web側の元index）を分離。
        ページ番号は既存の pageShift 計算がそのまま吸収する（KW0件でも -1 で整合）

■ Googleマップの帰属表示が切れていた（P9〜P12全マップ）
- 左下のGoogleロゴが弧だけ、右下「地図の誤りを報告する」が半分に切れて写り込み
- 対策: html2canvasだと確実に描画できないため、.gm-style-cc / ロゴを一旦隠してキャプチャし、
        マーカーと同じCanvas 2D直描画で「地図データ ©YYYY Google」を右下に描く
        （既存ナレッジ「Google Maps SVGマーカーはCanvas 2Dで直接描画」と同じ方針）

■ AI総評の誤り
- 言語比率「各1〜2%」← 実際0.9/0.9/0.5%。真因は analyze/route.ts の Math.round で
  2/218=0.92%→「1%」、1/218=0.46%→「0%」に潰していたこと。10%未満は小数1桁で渡すよう修正
- 競合「リスト2位以内の2店に次ぐ位置」← 口コミ数順位をAIに数えさせていたため非文になった。
  「口コミ数の順位: N店中M位（検索順位とは別物）」を確定値でプロンプトに渡す
- 「トップとの差は約84件まで縮まっている」← 1時点しかないのに推移を断定。
  プロンプトに「前回値が無いので推移の断定は禁止」を追加
- 口コミ総評がアクション率の話になっていた ← summary は口コミ限定ではなく当月全体の総評フィールド。
  見出しを「口コミ評価と今月の総評」に変更（実態に合わせた）

### 表示・レイアウトの修正
- P2「直近13ヶ月」なのに12行 … 計測開始前（対策開始月より前かつマップ0・アクション0）の
  先頭月が表からはみ出て切れていた。dispLabels/dispCharts で表示対象から除外しP3〜P5のグラフとも統一
- P7「直近5ヶ月」が1/2/3/4/6月（5月欠落）… 「計測実績のある直近Nヶ月」＋注記に変更
- P6の比較元が「4月」… 「※左側は直近で計測できた月との比較です」を明記
- ★が4.7でも5つ満点表示 … Math.round をやめ、幅%で部分塗りする2層表示に
- 総括ページ … 枠が flex:1 で85%空白＋Tailwind preflightで list-style が消えて番号が出ていなかった。
  listStyleType 明示＋flex解除＋当月サマリー（monthlyComment）を追加し「総括」の名に合う構成に
- 口コミ累計グラフのY軸が46,53,60…の7刻み … maxTicksLimit:6 でキリのよい刻みに
- 順位の色がページごとに緑/青/赤でバラバラ … 全ページ rankTextColor に統一（3位以内=青）。
  平均順位が一律赤（2.7の好成績も警告色）だったのも順位帯色に
- 競合比較 … 「#」→「検索順位」、「差分」→「自店との差」に改名＋定義注記。
  取得時点（7/30）とレポート対象月（6月）の自店実績が食い違う理由を注記で突き合わせ
- 「ポジティブ/ネガティブワード（推定）」← 顧客向けに「よく挙がる好評/改善ポイント」に
- 総検索数が上位20件の合計と誤読される … 「総検索数（全語句）」に
- 口コミ増加数の初月が無い理由を注記（前月差分のため集計開始月は対象外）
- 表紙（P1）だけページ番号が無かった → 追加

### 検算して問題なかった点（誤検知を避けるため確認済み）
- P1の前月比%は全6項目正しい（-2.6/-8.6/-20.5/-30.1/-15.8/-32.1%）
- P2の「合計」列＝WEB+ルート+通話+メニュー が全行一致、「アクション率2.15%」も一致
- P9の平均順位 9.9/13.1 は49地点の実測平均と一致
- P10の12.9/26.5 も「圏内のみ平均」として計算一致（＝仕様の問題であって計算ミスではない）
- P18の言語別集計（合計218・低評価23・構成比）はすべて一致
- P14の累計とP15の増加数は整合

### 検証
- tsc --noEmit エラーなし / next lint 新規警告なし / vitest 160件パス（rankTrend・rankCoverage・
  parseStartMonth・isYoyComparable のテスト22件を新規追加。旧バグは回帰テストで固定）
- next build 成功（227ページ）
- PDF実データで再計算し、誤りだった ↑6 / ↑6.2 / → がすべて「未計測」になり、
  正しかった ↓3 / ↓4.7 / ↓3.6 / ↓1.8 は変化しないことを確認

### 注意点・ハマったこと
- JSXの三項演算子の分岐内に {/* */} コメントを置くとパースエラーになる（要素が2つになるため）。
  /* */ だけにするか要素の内側に置く
- Tailwind preflight が ol/ul の list-style を none にするため、レポートのように
  素のstyleで組んだ箇所は listStyleType を明示しないと箇条書き記号が一切出ない
- PDF生成のKW絞り込みでは「スライド枠の順（slotIdx）」と「web側スライドのindex（kwIdx）」が
  別物になる。setGridKwIdx には元indexを渡さないと違うKWの地図をキャプチャする
- PDF生成で「地図は月セレクタの選択月・平均順位は最新月」という既存の不整合があった（同時に修正）
- avgRank は圏内地点のみの平均という仕様は report-api.ts(121,147) と spreadsheet.ts(499) の
  3箇所に同じロジックがある。変えるならこの3箇所セット（今回は表示側に圏内率を足す方針にした）

### 未対応・次回
- レポート表示は「当月は集計中→前月表示」仕様のため、修正の見え方確認は本番反映後に
  対象月2026/6のレポートを開いて行う
- AI総評の文面は既存の保存済みコメントには遡及しない（プロンプト修正は次回analyze実行分から）。
  過去分を直すには対象店舗で再分析が必要
- 既存の口コミ画面CSVに数式インジェクションの穴が残っている（前回からの持ち越し）
- PostgREST 1000行制限の疑いがあるクエリ4件（grid-export/pmax store-summary/diagnosis/posts）も未着手

━━━━━━━━━━ 以下、同日セッション後半の追記 ━━━━━━━━━━

### 修正版PDFでの検証（2回実施）

■ 1回目（18:21出力）で全19ページを確認 → 主要な指摘はすべて解消を確認
- 「一社イタリアン」の変動が ↑6(緑) → 「圏外へ」(赤)、「名東区パスタ」が → → 「圏外へ」
- 多地点の ↑6.2 → 「未計測」。P6/P7/P8で結論が一致するようになった
- ※「順位＝圏外」「多地点＝未計測」と表示が分かれるのは正しい。
  シートには2026/6が明示的に「圏外」と記録されており（outOfRangeで検出）、
  多地点グリッドの計測だけが無かったため。両方nullに潰していた旧実装では区別できなかった
- 圏内率 4/49(8%)・16/49(33%) が赤で併記、20→19ページ（空マップ2枚除外）、
  「地図データ ©2026 Google」表示、前年比バッジ消滅、AI総評「各1%未満」に修正

■ 1回目で新たなリグレッションを発見（自分の修正が原因）
- 競合比較ページ(P16)で、追加した突き合わせ注記のぶん内容が増え、
  20行の表が固定高794pxに収まらなくなった。
  slideBodyStyle は justifyContent:center のため溢れると【上下の両方】が切れ、
  見出し「口コミの競合比較（同エリア）」がヘッダーバーに隠れ、AI総評も下端で切れていた
- 対策(commit 9bc4ad0):
  ・表の行 padding 4px→2px、ヘッダ 6px→4px（20行で約80px削減）
  ・注記を fontSize 12→11 / lineHeight 1.6→1.45 に圧縮
  ・このページだけ justifyContent:flex-start にして、万一溢れても見出しは残る
    （下だけ切れる）安全な失敗の仕方にした
- 教訓: 固定高スライドに要素を足したら、そのページを必ずPDFで見ること。
  中央寄せのページは「溢れる=上下が同時に切れる」ので、タイトルが消えて気づきにくい

■ 2回目（18:33出力）で修正を確認 + 全ページ機械比較
- P16は見出し・20行・注記・AI総評すべて収まった
- 全19ページを前回出力とピクセル比較したところ11ページに差分が出たが、
  すべてAI総評の再生成による文言・行数の変化で、データと図表の数値は同一だった
  （P05の15%は総評が1行→2行になり図表が上に12pxずれたため）
- 検証スクリプト: PyMuPDFで両PDFを画像化し numpy で差分画素率を出す方法が有効

### AI総評の再分析について
- /api/report/analyze は withAudit("AI口コミ分析","PAID_OP") でJWT必須。
  cronシークレットでは叩けないため、Claude Code側からは実行不可。
  実行は画面から: /review-analysis → 対象月選択 → 「分析済みも再分析する」にチェック → 実行
  （チェックを入れないと当月分析済みはスキップされる）
- 再分析はプロンプト修正のデプロイ完了後に行うこと（先に流すと旧プロンプトで再生成される）

### 他社レポート（TRUE DESIGN CLINIC 2025年12月）のデザイン比較
Downloads\TRUE DESIGN CLINIC12月.pdf（10ページ・960×540の16:9）を確認。
■ 向こうが優れている点
1. GOOD/MORE構成: 指標ごとに「成果」と「課題→次回アクション」を対で見せる。
   自社は総括ページに改善策3つが並ぶだけで、どの指標の課題への打ち手か繋がらない
2. ヘッダーのカテゴリ色分け（検索=青/マップ=オレンジ/ユーザー反応=緑）
3. 最終ページ「弊社からのお願い」= 口コミ依頼・写真提供の依頼を定型セクション化
   （patty rôtiでも同じ依頼をメモ欄に手書きしていた＝定型化できる）
4. 検索語句を前月/今月の並置で見せ、順位変動を↑で示す
■ 自社が優れている点 / 真似しない点
- 情報量: 向こうは順位が1つも無い（多地点グリッド・競合比較・言語分析もなし）
- 「予約 0 / メニュー閲覧 0」に「変動なし」と成果カードを出している（無意味な指標表示）
- 全期間0の「予約」「メニュー」の行と凡例が残っている
  （自社は hasFoodMenus/hasBookings で出し分け済み）
- P8の下端が切れている（自社が同日直したのと同じ固定高オーバーフロー）

### セクション別カラー: 実装 → ユーザー判断で取り消し
- commit baa6856 で実装（SECTION_COLORS 8色 + slideBar()/stitle()/sectionBarBg()）。
  ヘッダーバーと見出しアクセント線を同色にし、配色はグラフ系列色と対応させた。
  白文字コントラストは全8色でWCAG AA(4.5:1)クリアを検算済み（最小 actions 5.50:1）
- ユーザー判断で取り消し → commit e7664bd で git revert
  （プッシュ済みのため履歴は書き換えずrevertコミットで対応）
- 色分け前 9bc4ad0 とソース差分ゼロを確認。復活させたい場合は git revert e7664bd の一発
- 設計メモ: slideBarStyle/stitleStyle はP-MAX側(pmax/[customerId], pmax-report-view)でも
  使っているため、既存exportは壊さず新関数を足す形にしてあった

### 最終状態
- main: e7664bd（レポート内容の修正はすべて残存、色分けのみ取り消し）
- 6e397c9 本体修正 / 9bc4ad0 P16溢れ修正 / baa6856 色分け / e7664bd 色分けrevert
- tsc・lint・vitest 160件・next build すべてパス

━━━━━━━━━━ Queency(21:21出力)レビューで発覚した3件の修正 ━━━━━━━━━━

■ 競合AI総評が事実と真逆（既存バグ・patty rôtiでは自店がリスト内だったため未発現）
- 症状: 自店341件より少ない店が13/20あるのに「自店は20店すべてを下回る」と生成
- 真因: analyze/route.ts の selfCount = comp.self?.reviewCount ?? 0。
  自店が検索上位圏外(comp.self=null)だと自店口コミ数が0扱い→全店が上回る判定に。
  表示側(client.tsx)は baseCount = comp.self?.reviewCount ?? displayTotalReviews で
  341件を使うため、表とAI総評が必ず矛盾する構造だった
- 修正: フォールバックを officialCount（表示側と同じ「レポート対象月末の自店口コミ数」）に統一。
  圏外時のプロンプトに「検索上位N圏外（口コミX件・評価Y）」と自店実数を明示し、
  「口コミ数の順位: 自店を含むN+1店中M位」を圏外時も必ず渡す
  （出さないとAIが自前で数えて誤る）。「検索上位圏外≠口コミ最下位」の注意も追加
- 検算: Queencyの表20店の実数で 旧=20店すべて上回る(誤)/新=多い7店・少ない13店・21店中8位。
  表の緑(+)13件と一致

■ 月次推移13行が固定高から溢れて最終行が切れる（前回修正が対症でしかなかった）
- 前回の「対策開始前の空月除外」はpatty rôtiでたまたま効いただけ。
  対策開始2022年12月のQueencyでは除外対象がなく13行フル→行高44px×14行=620px>可用580pxで
  2025/6行(39,490)が切れ、P1の前年比-67.4%の根拠数値が表から消えていた
- 修正: P2テーブルの行padding 10px→4px・ヘッダ12px→8px（13行時≈450pxで確実に収まる）。
  このテーブルはflex:1で伸縮するため、12行以下の月は行が自動で高くなり見た目は従来どおり
- 教訓: 「表示が直った」だけで完了にしない。原因（13行が収まらない）と症状（この店では
  13行にならない）を区別し、最悪ケース（13行フル）で成立する修正にする

■ P7の注記「※計測が無い月は列に含まれません」が事実と違った（自分が追加した文言）
- Queencyでは13列中10列が全KW「-」（シートに月の行だけあり中身が空）
- 修正: unifiedRankingHistoryで「全KWが順位なし・計測なし」の月列を除去してから直近13ヶ月を取る。
  【重要】当月(curLabel)列だけは空でも必ず残す — 消すと直近の順位あり月が最終列になり、
  過去の順位が「当月の順位」としてP6カードに表示されてしまう
- 注記は「※計測の無い過去月は列に含まれません」に修正（当月例外と整合）
- 検算: Queency 13列→4列 / patty 5列→4列(空の3月のみ除去) / 当月空でも当月列残存、の3ケースをnodeで確認

━━━━━━━━━━ 21:43出力の検証と残2件の修正 ━━━━━━━━━━
■ 21:43出力（再分析済み）で3件の修正を確認
- P2: 13行フル表示・2025/6(39,490)行が復活・合計列検算一致(2,849)
- P7: 13列→6列「計測実績のある直近6ヶ月」。残った2025/12・2026/3はシートに圏外記録あり
- P15: AI総評「上位20店を含む21店中、口コミ数は8位」= 事前検算と一致。トップ=i2 cafe(1,318件)も正
- 全18ページのピクセル比較で意図外の変化なし（差分は再分析による文言変化のみ）
■ 残っていた同根2件を修正
- P7セル: 計測済みで順位なしの月を「-」でなく「圏外」(13px灰)表示。
  「-」は未計測のみになり、列が残っている理由が読めるようになった
- analyze: AIに渡す推移系列を「N位/圏外/未計測」の3値化
  （シートoutOfRange + グリッド計測月で判定。旧: 一律「-」でAIが未計測月を圏外と作文）。
  共通ルールに「未計測の月を圏外・転落と書くの禁止」を追加
- 旧キャッシュ(outOfRange無し)では圏外→未計測側に倒れる（誤断定しない安全側）。
  キャッシュはレポート閲覧で再生成されるため自然に解消
■ 重要ナレッジ.txt に「圏外/未計測の区別」ほか同日の頻出パターン4件を追記
■ 未対応（機能追加のため別判断）: 総括のアクションとまとめの課題の対応付け（GOOD/MORE構成）

━━━━━━━━━━ CHILLRI堀江店(23:59出力)レビューと3件の修正（8/1未明） ━━━━━━━━━━
■ 前回2件の修正はCHILLRIでも動作確認
- P7セル「圏外」明示✓（2026/1-4が圏外表示・列が残る理由が読める）
- AI総評が計測月のみ言及✓（「2026/1の66位から2026/4に14位→2026/6圏外」未計測月への言及なし）
- P2 13行✓・圏内率0/49と49/49✓・帰属表示✓
■ 新たに3件発見→修正
1) 圏外→順位獲得が「初計測」（中立灰色）と表示されていた
   堀江冷麺: シートで1〜4月計測圏外→6月1位。大躍進なのに「初計測→1位」。
   rankTrend: 過去に順位なしでも計測(measured)があれば「圏内復帰」（緑）を返す。
   P6カード: prevOutMonth検出で「4月 圏外 ↑ 1位」表示に（prevRankのrankフォールバック抑止）。
   AI: kwDataのfirst判定にシート履歴(順位orOutOfRange)を追加→「前回圏外 圏内に復帰」で渡る。
   ※P8多地点テーブルはグリッド計測のみの系列なので「初計測」のままが正しい（ドメインが違う）
2) 対策開始月との前年比で「+3989.4%」「+6880.0%」
   開始月は月の途中から計測の不完全データ。isYoyComparable を < から <= に変更し
   開始「翌月以降」（フル計測月）とだけ比較。抑止時ラベルは「前年データが不完全なため」に統一
3) 競合検索結果が自店1件のみ（ニッチKW「堀江 冷麺」）
   1行だけの「比較」表＋AI「エリア内で優位な立ち位置」という比較相手ゼロの総評。
   client: 自店以外0件ならページごと非表示（totalPages連動）。analyze: 同条件でブロックをスキップ
■ テスト165件（CHILLRI実データの回帰テスト5件追加）・tsc・lint・build 全パス
