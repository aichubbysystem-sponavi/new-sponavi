===========================================================
2026-09-01 口コミ同期「ロケーション不明」328件の修正
===========================================================

## 症状
- 口コミ管理の全店舗同期後、「同期失敗店舗（328件）」が表示。全て「ロケーション不明」
  （= sync-reviews が status "api_404" を返した店舗。reviews/page.tsx:986）
- 同期自体は完了: 41336件同期・全814店舗完了・スキップ236件・エラー0件

## 真因
重要ナレッジ 2026-08-15 の既知パターンそのもの:
「GBPトークンは1本で全アカウントを見られない。見えないロケーションに
 v4 APIを叩くと404が返る（権限エラーではなく Not Found なので誤診しやすい）」

- report/sync-reviews と cron/sync-reviews はコメントで「全トークンを試す」と
  書きながら実装は getOAuthToken() の1本だけだった（allTokens = [token]）
- auto-post は2026-08-15に gbpFetchWithTokenFallback で修正済み。口コミ同期だけ直し漏れ
- 2026-08-28 に未登録アカウントの99店舗を business_groups INSERT→再同期で追加したため
  「既定トークンから見えない店舗」が一気に増えて328件になった

## 裏取り（ローカルから実測 2026-09-01）
- Go API /api/google/token の既定トークンで見えるのは 9アカウント / 518ロケーション
  (株式会社Chubby山田 / createfuture01・02 / JAPAN SELECT / Spotlight Navigator /
   株式会社Chubby 01・03・04・05)
- 失敗店舗（LUXBE NAIL 大阪梅田茶屋町店・六星麻辣湯by Sawada・DOMA&・
  梵 現代アートサロン 西中洲・LUXBE DOG 宮崎ナナイロ店・鳥は萬年 等）は
  この518件に1件も含まれない = 他のGoogleログイン配下
- 814店舗 - 518 ≒ 328件と数字も符合
- 本番は getAllOAuthTokens() で15アカウント/835ロケーション見える実績（2026-08-08ログ）
  → フォールバックすれば取れる

## 修正内容（コミット 411d167）
1. src/lib/gbp-token.ts
   - TOKEN_RETRY_STATUSES = [401, 403, 404] をexport
   - getFallbackTokens(): getAllOAuthTokens の5分TTLキャッシュ
     （全アカウントのリフレッシュは重いので店舗ごと/リクエストごとに叩かない）
   - アカウント→成功トークンのキャッシュ getAccountToken / setAccountToken
     （同一アカウント配下の2店舗目以降は正しいトークンで一発。失効時は自己修復）
2. src/app/api/report/sync-reviews/route.ts（手動同期）
   - fetchReviewsWithTokenFallback を新設。401/403/404のときだけ全トークンで順に再試行
   - 429等トークン無関係のエラーは再試行しない
   - 1ページ目成功後の途中エラーは再試行しない（トークン問題ではないため）
   - ページ途中エラー時の取得済み分保存（部分同期）は従来どおり維持
3. src/app/api/cron/sync-reviews/route.ts（毎時cron）
   - FetchResult に apiError?: number を追加（404も検出できるように）
   - 同じ fetchReviewsWithTokenFallback を適用
   - authError（連続3回で中断）はフォールバック全滅時のみ発火するようになった

## 検証
- next build OK / vitest 457件全パス
- push済み → Vercel自動デプロイ
- 最終確認（UI）: 失敗店舗1件で「この店舗を同期」→ 成功を確認後、全店舗同期で
  失敗件数が328件からどれだけ減るか確認する（このログ執筆時点では未実施）

## 期待値と残る404の読み方
- 修正後も404が残る店舗 = どの接続アカウントからも見えない店舗。原因は
  ①5つの接続Googleアカウントのどれにも権限がない
  ②そのアカウントのrefresh_tokenが失効（C-1のトークン再発行は7月から保留中）
  ③GBP側でプロフィール削除済み
  → 残った店舗名リストがそのまま調査対象になる

## 注意
- 初回の全店舗同期は404店舗ごとにトークン再試行が走るため従来より時間がかかる
  （アカウントキャッシュが効き始めれば2店舗目以降は高速）
- Chrome拡張（claude-in-chrome）は未接続だったため、検証はGo API直叩きで実施
